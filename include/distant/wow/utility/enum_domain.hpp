#pragma once

namespace distant::wow::utility
{
	/*template <typename T>
	constexpr bool enum_domain_check(int check)
	{
		return false;
	}

	template <>
	constexpr bool enum_domain_check<object_type>(int check)
	{
		return static_cast<int>(object_type::local) <= check && check <= static_cast<int>(object_type::object);
	}*/
}
